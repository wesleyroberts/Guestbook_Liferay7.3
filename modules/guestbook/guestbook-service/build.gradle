dependencies {
	api project(":modules:guestbook:guestbook-api")

	compileOnly group: "com.liferay.portal", name: "release.portal.api"
}

buildService {
	apiDir = "../guestbook-api/src/main/java"
}

group = "guestbook"